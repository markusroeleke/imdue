# Normalize line endings for all text files to LF on checkout
* text=auto eol=lf

# Windows batch/PowerShell scripts need CRLF
*.bat  text eol=crlf
*.ps1  text eol=crlf

# Explicitly binary — never touch line endings
*.pdf  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.ico  binary
